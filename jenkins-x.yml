buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: gcr.io/jenkinsxio/builder-go-maven
        stages:
          - name: bdd-stage
            steps:
              - name: git-init
                command: jx step git validate
              - name: bdd
                command: make bdd
